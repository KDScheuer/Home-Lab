services:
  homepage:
    image: nginx:alpine
    container_name: homepage
    restart: unless-stopped
    environment:
      TZ: America/Denver
    volumes:
      - /srv/homepage:/usr/share/nginx/html:ro
    networks:
      - proxy

networks:
  proxy:
    external: true
