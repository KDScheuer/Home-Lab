.env
*.env
todo
# Terraform state files - never commit these
terraform.tfstate
terraform.tfstate.backup
*.tfstate
*.tfstate.*
 
# Variables file containing secrets
terraform.tfvars

# Ansible secrets — not committed; see .env.example at repo root for required values
ansible/inventory/group_vars/all/secrets.yml
 
# Terraform working directory
.terraform/
.terraform.lock.hcl

tfplan*
tfplan.*

# Crash logs
crash.log
crash.*.log
 
# Override files
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# homepage icons & images
/helm/apps/homepage/*.jpg
/helm/apps/homepage/icons/*.png
