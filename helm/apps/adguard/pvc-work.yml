apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-work
  namespace: adguard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: adguard-work
  resources:
    requests:
      storage: 10Gi