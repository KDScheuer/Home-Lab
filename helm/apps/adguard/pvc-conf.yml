apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: adguard-conf
  namespace: adguard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: ""
  volumeName: adguard-conf
  resources:
    requests:
      storage: 1Gi